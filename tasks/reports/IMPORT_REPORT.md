# 数据导入报告

## 导入概览

| 项目 | 详情 |
|------|------|
| 导入时间 | 2026-03-07 23:03:52 ~ 23:07:30（含关联修复） |
| 总耗时 | 约 4 分钟 |
| 执行脚本 | `scripts/import_data.py` + `scripts/fix_material_customer.py` |
| 后端地址 | http://localhost:8080 |

## 导入结果

### 客户档案

| 指标 | 数量 |
|------|------|
| Excel 总行数 | 487 行（含表头1行） |
| 成功导入 | **480 条** |
| 跳过（代码/名称为空） | 6 条 |
| 失败 | 0 条 |

### 工艺数据

| 指标 | 数量 |
|------|------|
| Excel 总行数 | 156 行（含表头1行） |
| 成功导入 | **155 条** |
| 跳过（代码/名称为空） | 0 条 |
| 失败 | 0 条 |

### 物料档案

| 指标 | 数量 |
|------|------|
| Excel 总行数 | 23166 行（含表头1行） |
| 成功导入 | **23164 条** |
| 跳过（代码/名称为空） | 1 条 |
| 失败 | 0 条 |

### 物料客户关联修复

| 指标 | 数量 |
|------|------|
| 成功关联 customerId | **22864 条** |
| 客户名称匹配不到（历史数据） | 146 条 |
| 无 customerName（空白） | 154 条（原本就无需关联） |

## 验证结果（接口查询）

| 模块 | 接口查询数量 | 备注 |
|------|-------------|------|
| 客户 | **481** | 480 条新导入 + 1 条原有测试客户 |
| 工艺 | **155** | 与导入数量完全一致 |
| 物料 | **23164** | 与导入数量完全一致 |

## 问题说明与处理

### 问题1：物料客户关联映射字段名不一致
- **现象**：`/api/customers/all` 接口返回字段名为 `name`，而非 `customerName`，导致首次导入时客户映射加载失败（只加载了 1 条）。
- **处理**：导入完成后，单独运行 `fix_material_customer.py` 脚本，重新加载客户映射（使用正确的 `name` 字段）并批量更新 23164 条物料的 `customerId`。
- **结果**：22864 条物料的 `customerId` 成功关联。

### 问题2：146 条物料无法匹配客户
- **原因**：这些物料的 `customerName` 在客户档案中不存在，属于历史遗留的已删除/重命名客户数据。
- **影响**：这 146 条物料的 `customerName` 字段有值（可查询），但 `customerId` 为 null（无外键关联）。
- **建议**：可通过系统界面手工核对这 146 条物料，视业务需要决定是否关联到现有客户。

## 总结

✅ 导入**全部成功，零失败**，共导入：
- **客户 480 条**（487行 - 表头1行 - 空行6行）
- **工艺 155 条**（156行 - 表头1行）
- **物料 23164 条**（23166行 - 表头1行 - 空行1行）

总计 **23799 条**数据成功写入系统。
